# Decisions

Format: `YYYY-MM-DD — Decision — Reason — Who`

- 2026-09-17 — iOS native first (Swift/SwiftUI), Android Phase 2 — Apple on-device speech + Foundation Models need the least code; iPhone buyers pay for one-time apps — Jeremy
- 2026-09-17 — v1 templates: General meeting/lecture, Client/consulting meeting, Contractor walk-through — Matches owner's consulting work and contractor niche — Jeremy
- 2026-09-17 — Business model: free download, 3 free summaries, one-time unlock; no subscription — No server costs — Jeremy
- 2026-09-17 — v1 strictly on-device (no Private Cloud Compute, no third-party LLM APIs) — Privacy promise is the core pitch — Jeremy
- 2026-09-17 — Product name is VeraFlow; Riffle stays as the alpha/demo codename (TestFlight alpha display name, demo mode, fixtures). Code identifiers and product IDs use VeraFlow — Avoids a rename across code later; testers can tell alpha builds apart — Jeremy
- 2026-09-17 — Xcode project is generated from `project.yml` with XcodeGen; `VeraFlow.xcodeproj` is gitignored — M0 was written without access to Xcode; a YAML definition is reviewable and reproducible, and XcodeGen is a dev tool, not an app dependency (the FluidAudio-only rule is about linked packages) — Jeremy / Claude Code
- 2026-09-17 — Alpha builds use the same bundle ID as release, differing only in display name ("Riffle (alpha)") and the `ALPHA` compile flag — Keeps StoreKit product config and TestFlight simple; side-by-side installs aren't needed — Jeremy / Claude Code
- 2026-09-17 — FluidAudio pinned to exact version 0.15.7 (latest tag at time of M0) — SPEC §10.1 asks for an exact pin; bump deliberately in M4 after checking release notes — Claude Code
- 2026-09-17 — `AppServices.live()` returns fakes until each milestone lands its real service — Lets the app launch and the UI be built against protocols from M0; each milestone swaps one service — Claude Code
- 2026-09-17 — Building with Xcode 26.6 / iOS 26.5 SDK until Xcode 27 ships — That is what is installed; nothing through M4 needs iOS 27 APIs. M5 must gate `SystemLanguageModel.contextSize` and `tokenCount(for:)` behind `#available(iOS 27, *)` and also compile-time `#if canImport` checks if the SDK still lacks them — Jeremy / Claude Code
- 2026-09-17 — M1 recorder uses the classic `AVAudioEngine.connect`, `installTap`, and `AVAudioSession.interruptionNotification` APIs — Apple's docs mark them deprecated in iOS 27 in favour of `connectNode`, `AVAudioSession.InterruptionContext`, and friends, but those replacements are iOS 27-only and absent from the iOS 26.5 SDK we build with. Revisit when Xcode 27 is in use: gate the new calls behind `#available(iOS 27, *)` — Claude Code
- 2026-09-17 — Recorder graph is a tap on `inputNode` in its native format, converted per buffer with `AVAudioConverter` to mono 44.1 kHz float; no output path — First attempt routed input → mixer → muted main mixer and tapped the mixer, and on device the file came out silent. Tapping the input directly is Apple's documented pattern and doesn't depend on the engine pulling audio through an output — Claude Code
- 2026-09-17 — Launch recovery marks an interrupted recording `.failed` (not `.recorded`) when its audio file is missing or unreadable — SPEC §8.2 says mark `.recorded`, but a row that can't play or transcribe would just fail later with a worse message; readable files are marked `.recorded` as specified — Claude Code
- 2026-09-17 — Capture format is AAC in an ADTS stream (`audio.aac`), not AAC in CAF as SPEC §8.2 says — Verified on device: a force-quit mid-recording left the CAF/AAC file unreadable (181 KB on disk, AVAudioFile refused it) because Core Audio writes the CAF packet table only on close. ADTS frames each carry a header, so a truncated file plays up to the last whole frame; same codec, sample rate, and ~64 kbps. Unit test truncates an ADTS file and reads it back. Rows created before this change keep their `audio.caf` name and still resolve — Jeremy / Claude Code
- 2026-09-17 — Share-sheet import uses document types + `onOpenURL` (system copies the file to Documents/Inbox), not a Share Extension — Same user-visible result ("VeraFlow" in the share sheet / "Open in"), no second app target, no app group, no extra signing. Revisit only if a "share without leaving the other app" flow is wanted — Jeremy / Claude Code
- 2026-09-17 — Imported files keep their original container (`audio.m4a`, `audio.wav`, ...) rather than being transcoded to ADTS — The crash-safety reason for ADTS applies to live capture only; playback and transcription read any of the supported containers, and copying is instant where transcoding a 60-minute file is not — Claude Code

- 2026-09-17 — Every restart of capture (Resume, configuration change, media-services reset) discards the `AVAudioEngine`, builds a new one, re-reads the input node's output format (retrying briefly while it reads 0 Hz after a route change), and only then installs a tap — The 90-minute device test was interrupted at 43:42 and crashed on Resume. The crash log shows an Objective-C exception inside `installTap`, reached from the configuration-change handler re-tapping an engine that Resume had just started. `installTap` cannot be caught from Swift, so the conditions it rejects are checked first and the engine is always stopped before re-tapping; a bad state degrades to "Paused, tap Resume" instead of a crash. A fresh engine is required, not just a stopped one: re-tapping a running engine is what raised the exception. A first version also demanded that the node's output format equal the hardware format; on device those legitimately differ after a Bluetooth switch (session at the headset's 16 kHz, hardware reporting the iPhone mic's 48 kHz), which left Resume stuck, so the tap simply uses the node's output format as Apple's samples do. The interruption reason is now logged so the next pause can be explained — Claude Code
- 2026-09-17 — Playback duration comes from `AudioFileInfo.duration(of:)` (frame count), not `AVAudioPlayer.duration` — For ADTS streams the player's duration is a bit-rate estimate and showed 42:22 for a 43:42 recording; the frame count matches the recorder's timer — Claude Code
- 2026-09-17 — Recording Live Activity (Lock Screen banner + Dynamic Island: timer, Recording/Paused, bookmark count) is built in M1, status only, as a `VeraFlowWidgets` app-extension target sharing `Shared/RecordingActivityAttributes.swift` with the app — SPEC §16 lists it as a v1.1 stretch item; Jeremy asked for it after the first long device test. Status only (no Lock Screen buttons) keeps it to ActivityKit + one widget target; buttons would add App Intents and are a later step. The extension has no network, no app group, and no push token — Jeremy / Claude Code
- 2026-09-17 — With a Bluetooth headset connected, the recorder prefers the iPhone's built-in mic unless the user picks the headset (or "Automatic") in the Microphone menu; the choice is remembered — iOS otherwise routes recording through the headset's 16 kHz call profile, which sounds worse than the phone for a meeting, and on device the earbuds' mic went silent whenever they weren't worn. The menu is on the Record screen before and during recording; switching mid-recording restarts capture on the new input without a pause — Jeremy / Claude Code
- 2026-09-17 — Transcription runs behind `EngineSelectingTranscriptionService`, which routes each call to Apple Speech (default) or FluidAudio Parakeet according to a `UserDefaults` preference that only the DEBUG "Transcription benchmark" screen (Settings → Developer) can change — SPEC §9.4 asks for Parakeet behind a debug flag and a benchmark on the fixtures. One selector in front of two real services means the pipeline, the progress events, and the transcript screen are identical for both engines, so the comparison is fair and switching the shipped engine later is one line in `AppServices.live`. Release builds always use Apple Speech until the benchmark result is recorded here — Jeremy / Claude Code
- 2026-09-17 — Apple Speech asset downloads and `DictationTranscriber` fallback are handled inside `LiveTranscriptionService`; the reserved locale is kept for the app's lifetime rather than released after each file — SPEC §9.2 suggests releasing unused locales, but the app only ever transcribes in the user's locale and re-reserving it per file adds a slow, failure-prone step for nothing. Revisit if a per-recording language choice is added — Claude Code
- 2026-09-17 — Background processing uses `BGContinuedProcessingTaskRequest` (iOS 26) with the `.fail` strategy and an inline fallback when the system won't grant it; Core ML engines (Parakeet now, the diarizer in M4) may still be throttled in the background on iOS 27 without the `com.apple.developer.background-tasks.continued-processing.inference` entitlement, which we don't hold yet — Apple Speech is not affected, so the shipped pipeline works either way; the entitlement request is queued for M4 when the diarizer lands. Until then, the benchmark screen tells the tester to keep the app in the foreground — Claude Code
- 2026-09-17 — Transcript search is a text field in the transcript header, not `.searchable` — The detail screen is pushed from the Library, which already owns the navigation bar's search field; a second `.searchable` on a pushed view fights it on iOS 26. The in-page field also keeps the match count and the Edit button together — Claude Code
- 2026-09-17 — The M4 aligner (`LiveTranscriptAligner`) is a port of the Antigravity branch's `TranscriptAligner` (largest overlap, nearest turn within 0.5 s, previous-speaker fallback, short-run smoothing, keys by first appearance), re-fitted to our `TranscriptAligning` protocol and `Paragrapher.Rules`, with its six tests ported and three added — The review found that module sound; porting it with credit beats rewriting it. The rest of that branch stays unmerged as reference — Jeremy / Claude Code
- 2026-09-17 — Diarization never blocks a recording: on any failure (models missing, download refused, FluidAudio error) the transcript keeps a single "Speaker 1", the stage still advances to `.diarized`, and the reason is kept in `failedStage`/`failureMessage` so the transcript shows "Speaker labels couldn't be added" with Retry — SPEC §6.3 and M4's "failure path shows a single speaker + retry". Marking the row `.failed` would have hidden a perfectly readable transcript behind an error — Claude Code
- 2026-09-17 — When the user has already edited a transcript, diarization labels the existing paragraphs by majority vote of their words (`speakerKeys(forSegments:turns:)`) instead of rebuilding paragraphs by speaker — Re-splitting would discard the edits. Unedited transcripts are re-split per SPEC §10.2 so speaker changes start new paragraphs — Claude Code
- 2026-09-17 — `LiveDiarizationService` loads FluidAudio's `OfflineDiarizerModels` once per launch and creates a fresh `OfflineDiarizerManager` per file with the "Expected speakers" hint in `config.clustering.numSpeakers` / `minSpeakers` — The manager isn't Sendable and its config is fixed at init; the models struct is Sendable and holds the expensive part. "4 or more" maps to `minSpeakers = 4` rather than forcing an exact count — Claude Code
- 2026-09-17 — M5 is split: the deterministic half (due dates, chunking, budget, post-processing, prompts, the summarizing stage, the Summary tab) is written against the `SummarizationService` protocol with the fake, and the Foundation Models service comes after the M3/M4 compile round — Every Foundation Models API name must be verified against the SDK on Jeremy's Mac (CLAUDE.md), and the iOS 26.5 SDK may lack iOS 27 calls. Everything else in §11 is plain Swift and fully testable now. Until the real service lands, `AppServices.live` keeps the fake summarizer, which is clearly labelled "FakeSummarizationService" in each summary's Model row — Claude Code
- 2026-09-17 — The summarizing stage never fails a recording: with the on-device model unavailable (or a generation error) the row still becomes `.ready`, the reason is kept in `failedStage`/`failureMessage`, and the Summary tab offers Retry — SPEC §15: transcripts must work on every iPhone; a `.failed` row would hide a good transcript. The stage also leaves an earlier non-fatal speaker-label reason in place — Claude Code
- 2026-09-17 — `LiveDueDateResolver` resolves "next <weekday>" to that weekday in the following Monday-to-Sunday week, "next week" to Monday, "end of week" to Friday, a bare weekday to its next occurrence (today included), phrases without a time to 5 PM, and month/day phrases to their next occurrence after the recording date; `NSDataDetector` is only a fallback and only its month and day are used — SPEC §11.6 fixes some of these; the rest follows common business usage and the Antigravity review's corrections. All of it is table-tested against a fixed Thursday so results never depend on the day the tests run — Claude Code
- 2026-09-18 — `LiveSummarizationService` catches `LanguageModelSession.GenerationError` (iOS 26 API) and uses `GenerationOptions(sampling:temperature:maximumResponseTokens:)` — Apple's reference now marks both deprecated in the iOS 27 SDK (replaced by `LanguageModelError.contextSizeExceeded` and the `samplingMode:` initializer, back-deployed), but we build with Xcode 26.6 / iOS 26.5 SDK where only the older names exist. Apple says apps must migrate before submitting with Xcode 27, so this is a listed M9 task. `contextSize` is back-deployed to iOS 26.0 and is read at runtime; `tokenCount(for:)` (iOS 26.4+) is used behind `#available` to count instructions and schemas and to calibrate the chars ÷ 3.5 estimate with one real count of the transcript — Claude Code
- 2026-09-18 — Chunking uses a synchronous token estimate scaled by one real `tokenCount` of the whole transcript, never below the conservative estimate — Counting every line asynchronously would cost hundreds of model calls per summary; one calibration call keeps chunks honest while the chunker stays pure and testable — Claude Code
- 2026-09-18 — Reminders integration asks for **full** access (`requestFullAccessToReminders`, `NSRemindersFullAccessUsageDescription`) rather than write-only — Listing the user's lists so they can choose one (SPEC §12 "chosen list") needs read access; write-only would only allow adding to the default list. Nothing is read back except the list names — Claude Code
- 2026-09-18 — PDF export is laid out with Core Text (`UIGraphicsPDFRenderer` + `CTFramesetter`) from the same sections as the plain-text export, not `ImageRenderer` of a SwiftUI view — Pagination with page numbers is what SPEC §12 asks for, and `ImageRenderer` produces one tall page; this runs off the main thread and is unit-tested in the Simulator — Claude Code
- 2026-09-18 — Export files are written to the app's `tmp/Exports/` and shared with `UIActivityViewController` rather than `ShareLink` — The share menu has five formats; building all of them up front for `ShareLink` (which needs its item at render time) would generate a PDF and an `.m4a` on every menu open — Claude Code
- 2026-09-18 — The Diagnostics screen stays visible once it has been revealed by seven taps on the version number — SPEC §15 describes the gesture; re-hiding it after every launch would make support conversations harder ("tap it seven times again"). It's still absent for anyone who never finds it — Claude Code
- 2026-09-18 — The app lock uses `.deviceOwnerAuthentication` (Face ID / Touch ID with passcode fallback), not `.deviceOwnerAuthenticationWithBiometrics` — A user whose Face ID fails must still be able to open their own recordings; the passcode is the system's own fallback. The toggle is disabled on devices without a passcode because there is nothing to lock behind — Claude Code
- 2026-09-18 — UI-test launches (`--use-fake-services`) mark onboarding complete and turn the consent reminder and app lock off; `--show-onboarding` opts back into the onboarding flow — The existing recorder and library UI tests start on the Library and tap Start Recording immediately; gating them behind onboarding and the consent sheet would test the wrong thing — Claude Code
- 2026-09-18 — On iPhones without Apple Intelligence the paywall stays available but says up front that AI summaries don't work on that iPhone and sells the exports, Reminders, and email drafts (plus summaries on a future eligible iPhone) — SPEC §13.3 / §19 left this open. Hiding the unlock would strand people who mainly want exports; selling it silently would mislead. Clear copy on the screen itself was the honest middle — Claude Code (for Jeremy to confirm)
- 2026-09-18 — The free-summary limit is enforced in the pipeline's summarizing stage, not only in the UI — Summaries are generated automatically after diarization, so the fourth one has to stop inside the pipeline; the recording still becomes `.ready` with a reason and the transcript stays fully usable (SPEC §13.2: transcripts are unlimited) — Claude Code
- 2026-09-18 — Paywall price and product copy come from StoreKit at runtime; `VeraFlow.storekit` carries a $24.99 placeholder for local testing only — SPEC §13.1 leaves the price to decide after TestFlight; nothing in the app hardcodes it — Claude Code
- 2026-09-18 — `LiveBackgroundProcessing` waits at most 3 s for iOS to launch the continued-processing task, then runs the work in the foreground — On Jeremy's iPhone (iOS 27.0) built with the iOS 26.5 SDK, `BGTaskScheduler.submit` returns without error but the scheduler's XPC reply can't be decoded ("value for key 'NS.objects' was of unexpected class 'NSSet'") and the launch handler never fires, which left every recording at "Waiting to process". The two paths are mutually exclusive through a claim flag. Re-check when building with Xcode 27 — Claude Code
- 2026-09-18 — Live Activity gains Pause/Resume and Bookmark buttons via `LiveActivityIntent`s in `Shared/`, delivered to the recorder through a main-actor `RecordingControlHub`; playback gets Lock Screen / Control Center controls through `MPNowPlayingInfoCenter` + `MPRemoteCommandCenter` — Jeremy asked for both after the first full-pipeline device run. `LiveActivityIntent` performs inside the app process, so no app group or shared storage is needed; the recorder view model registers a handler while recording and clears it on Stop — Jeremy / Claude Code
- 2026-09-18 — After an unlock, summaries that stopped at the free limit are re-run automatically (`retrySummariesBlockedByFreeLimit`), and the toolbar Share menu receives the unlock state as a parameter rather than reading `AppState` from the environment — On device the toolbar menu kept opening the paywall after the purchase: toolbar menu content doesn't reliably see environment objects. A rate-limited model reply is now retried twice after 5 s instead of failing the summary — Claude Code
- 2026-09-18 — Apple's on-device model rate-limits aggressively on device ("Client rate limit exceeded", even for `contextSize`, which then reads 0). The summarizer now assumes 4,096 tokens when the size lookup fails, caches instruction/schema token counts per launch, skips calibration while the model is unhealthy, backs off 10 s then 30 s, and finally reports `.rateLimited`; the pipeline keeps the recording usable and retries the summary by itself 3 minutes later — Seen on Jeremy's iPhone after two successful summaries; the 0-token fallback had been producing 200-token chunks and *more* requests. Request count per summary is now 1 calibration + map/reduce/final calls — Claude Code
- 2026-09-18 — `LiveDiarizationService` loads cached models on demand inside `diarize` — On the second launch `modelsReady()` was true (files on disk) so the pipeline skipped `prepareModels`, and every diarization failed with "models aren't downloaded yet" — Claude Code
- 2026-09-18 — Rate-limit retries back off (base wait doubling to 8×), fire on the next foreground if their wait elapsed while the app was suspended, and "Process next" (row menu, Summary Retry) moves a recording to the front of the queue and retries its failed stage immediately — Jeremy asked for background retries; iOS suspends the app so a timer can't fire there, and the continued-processing task is unusable until Xcode 27 (see the 2026-09-18 entry). Foreground catch-up is the honest equivalent — Jeremy / Claude Code
- 2026-09-18 — Recording a Teams/Zoom call (headphone audio + Bluetooth mic) is out of scope: iOS lets no third-party app capture another app's audio, and the call app owns the microphone. mp4 / mov / m4v files can be imported instead; the audio track is extracted with `AVAssetExportSession` (AppleM4A preset) into `audio.m4a` — Import is the only route for online meetings on iPhone; extraction keeps everything on device — Jeremy / Claude Code
- 2026-09-18 — Pausing a recording keeps the audio engine running and only stops the tap writer from writing (`TapWriter.isPaused`); Resume flips the flag back, and rebuilds the engine only if it actually stopped (interruption, route change). The Lock Screen banner shows full-width, `.large` Pause/Resume and Bookmark buttons on their own row — On device, Lock Screen Pause worked but Resume didn't: pausing the engine ends audio I/O, iOS suspends the app, and a backgrounded app may not *start* recording again. Keeping I/O alive means the orange mic indicator stays on while paused; nothing is written to disk during the pause and the elapsed time (frames written) stands still. The old buttons were too small for a thumb — Jeremy / Claude Code
- 2026-09-18 — The recorder remembers the chosen microphone (`wantedInputID`) and calls `setPreferredInput` only on an active session: right after `setActive(true)` in `start()` and in every capture restart, and immediately when the user switches mid-recording. Before, the choice was applied on the Record screen while the session was still inactive — Apple's `setPreferredInput` reference: "You must set a preferred input port only after setting the audio session's category and mode and activating the session." On device the menu said iPhone Microphone while the route was the Bluetooth headset, and switching did nothing. The pure decision (`AudioInputRouting.action`) is unit-tested; the session call itself can only be checked on a phone — Jeremy / Claude Code
- 2026-09-18 — Videos shot on the iPhone are imported through the system Photos picker ("Import Video from Photos", `PhotosPicker` filtered to videos), copied to `tmp/PhotoImports/` via a `Transferable` file representation, then run through the same mp4/mov audio extraction as a Files import — The Files picker can't see the Photos library, which is where Jeremy's own videos live. The Photos picker runs out of process, so the app needs no photo-library permission and never sees anything but the chosen files — Jeremy / Claude Code
- 2026-09-18 — Any `Menu` on the Record screen lives in its own `View` that reads only the state it shows (`MicrophoneMenu`), never in the body that renders the 100 ms timer and level meter — On device an open microphone menu was being rebuilt under the finger by the snapshot redraws and taps were silently dropped (the log showed the menu opening with no action firing). The same rule applies to any future control on a screen with a live meter — Jeremy / Claude Code
- 2026-09-18 — `LiveDiarizationService` turns on FluidAudio's `zeroVoteReembed` post-pass (off by default upstream) — On the 4:40 device recording with "Expected speakers: 3", clustering produced 3 centroids but the third speaker held 2 of 138 windows and reconstruction absorbed its turns into the dominant speaker (`Clustering completed … 3 centroids` then `diarized … 2 speakers`). FluidAudio documents this pass as the fix for exactly that: frames with no clustering votes are re-embedded and assigned to the closest centroid. Cost is a few extra embedding calls per file. Not unit-testable without FluidAudio in the test target; verified on device — Claude Code
- 2026-09-18 — Security review fixes (`docs/reviews/2026-09-18-security-review.md`): with the app lock on, an opaque cover appears the moment the scene goes inactive and becomes the lock in the background; launch arguments are compiled only into DEBUG builds; Now Playing shows "Recording" instead of the title while locked; clipboard copies are local-only and expire in two minutes; export files get complete file protection and are deleted when the share sheet closes; `tmp/Exports`, `tmp/PhotoImports`, and `Documents/Inbox` are swept at every launch; the SwiftData store keeps its default path but is now protected `completeUntilFirstUserAuthentication` and excluded from backup like the audio, and the FluidAudio model cache is excluded from backup; Parakeet and the engine selector exist only in DEBUG; model-written text is stripped of links and markup before it becomes a reminder or an email — Jeremy asked for the app to be deployable in a DoD environment. The store's class stays `completeUntilFirstUserAuthentication` (not `complete`) because the background pipeline writes transcripts while the phone is locked. The Alpha configuration keeps `DEBUG` so testers get the benchmark screen; only Release is the vetted binary. FluidAudio 0.15.7 resolves to commit `41540ea237350afe5117a082b5c28eda642d0612` — Jeremy / Claude Code
- 2026-09-18 — The privacy cover only appears when the app lock is enabled — Users who never turn the lock on would otherwise see the app blank out on every Control Center pull; the cover is part of the lock feature, and an MDM profile that requires the lock gets the cover with it. Bundling the diarizer models (removing the Hugging Face download) is left as a decision for Jeremy: it adds roughly 60 MB to the download — Claude Code
- 2026-09-18 — Accessibility review fixes (`docs/reviews/2026-09-18-accessibility-review.md`): every fixed font size became a `@ScaledMetric`; the recorder timer and level meter expose spoken values with `.updatesFrequently`; recording state, notices, pipeline stages, failures, search-match counts, importing, and paywall messages are announced; each transcript paragraph and action item is one VoiceOver element whose long-press and tap actions are in the actions rotor; speaker colours, the alert red, and the accent moved to asset-catalog colour sets tuned to at least 4.5:1 in light and dark; touch targets were raised to 44 pt (28 pt minimum where a 44 pt control would break the layout, with the 44 pt action on the enclosing row); the onboarding pages and consent sheet scroll; exports carry the AI disclaimer and PDFs carry title metadata — Jeremy asked for Section 508 conformance. Tagged PDF structure was left for a follow-up: it needs the PDF composer to draw each heading and paragraph as its own tagged run instead of one Core Text frame — Jeremy / Claude Code
- 2026-09-18 — No entitlements file for now: declaring `com.apple.developer.default-data-protection` made automatic signing fail on Jeremy's Personal Team ("provisioning profile doesn't match the entitlements file"), because only paid Developer Program App IDs can carry the Data Protection capability — The declaration changes nothing at runtime (iOS already defaults to `CompleteUntilFirstUserAuthentication`, and `DataProtection.swift` sets classes explicitly), so it comes back when the app moves to a paid account for TestFlight, which it needs anyway — Claude Code
- 2026-09-18 — The diarizer models load with `MLComputeUnits.cpuAndNeuralEngine`, and a diarization failure while the app is in the background parks the recording at `.transcribed` and re-queues it on the next foreground instead of recording a failure — On device, every segmentation call failed with "Insufficient Permission (to submit GPU work from background)" when Jeremy left the app right after Stop: iOS refuses Metal work from a backgrounded app, and FluidAudio's default `.all` compute units put the models on the GPU. The Neural Engine is not subject to that rule. The foreground retry is the safety net in case a model still falls back to the GPU — Claude Code
- 2026-09-18 — The redesign (Chalk light / Slate dark, `docs/design/DESIGN_SPEC.md`) is applied through the supplied tokens and components in `VeraFlow/Design/`; screens reference `VFColor` / `VFText` / `VFSpace`, never hex values. Fonts are static instances of Newsreader and Manrope generated from Google's variable fonts with fontTools (`instancer`, wght 400/500/600/700, Newsreader at optical size 16), bundled under the SIL Open Font License with both licence texts — The design package asked for static instances because iOS handles variable fonts inconsistently; Google ships only the variable files. PostScript names were set to the ones `VFFontName` expects and verified in the name table. Jeremy checks on device that titles render in Newsreader; a silent fallback to San Francisco means a name mismatch — Jeremy / Claude Code
- 2026-09-18 — Library cards take their title and one-line gist from the recording's summary (`SummaryPayload.title` / `overview`) and fall back to the recording title and first transcript line — Spec open question 1 asked who generates titles; the summary model already does, so no second model call. The Library hides the system navigation bar and draws the design's header; the three UI tests that waited on the bar now wait on the serif title. Sort and the import entries moved into a "More" round button next to search and settings, a small addition to the spec's two buttons — Claude Code
- 2026-09-18 — The Record screen offers the summary template before recording (`AppPreferences.defaultTemplate`, remembered as the default and shown again in Settings), and the Audio tab replaces the old Audio list: waveform scrubber with peaks read from the file (`WaveformPeaks`, off the main actor, drag or VoiceOver-adjustable by 15 s), ±15 s and speed, per-voice talk time (`TalkTime`), marks, details, and Export transcript / Share audio pills. Bookmarks are called "marks" on screen, matching the design — Spec §4 puts template selection before recording; the template still changes afterwards from the Summary tab. The talk-time rows are hidden for one speaker (spec §5). The old segmented control, system navigation titles, and `Form`s are gone from Record, Settings, the consent sheet, and the paywall; each draws the design's own header, so those screens hide the system bar and pushed screens (Diagnostics, benchmark) get it back — Claude Code
- 2026-09-18 — The processing card (spec "Processing") shows four steps derived by `ProcessingSteps` from the recording's stage, in the Summary and Transcript tabs while there is nothing to read. Its copy says transcription keeps going in the background and that speaker labels and the summary finish while the app is open — The spec's line "you can close the app" isn't true on this build: diarization is deferred to the foreground (GPU rule) and the continued-processing task is unusable until Xcode 27, so the card says what actually happens. The "Read what's ready" button isn't needed: the tabs are already reachable — Claude Code
- 2026-09-18 — Settings gains an Appearance row (System / Light / Dark, `AppPreferences.appearance`, applied with `preferredColorScheme` at the root so sheets follow), and Library search looks through the stored title, the summary's generated title and gist, the tags, and the transcript text, requiring every typed word to appear somewhere — The design spec §2 said "no in-app theme picker in v1"; Jeremy asked for one after seeing both appearances. Search had matched only the stored title, which the card no longer shows once a summary exists, so it looked broken; Jeremy also wanted transcript hits — Jeremy / Claude Code
- 2026-09-19 — Model errors are classified by kind through `LanguageModelErrorBridge`, which recognises the iOS 27 `FoundationModels.LanguageModelError` cases by the case name in the error's description, since this iOS 26.5 SDK build can't name that type; a `timeout` shrinks the chunks and retries like a context overflow, and when that still fails the recording is parked with `SummarizationError.timedOut` and retried automatically on the rate-limit schedule — On Jeremy's iPhone (iOS 27.0) a real recording's summary failed twice: "LanguageModelError error -1" after 110 s (the framework's new error type fell through to the generic catch as an opaque failure) and then the rate limiter. The bridge is the stop-gap until the Xcode 27 migration replaces it with the typed cases; it is unit-tested against a stand-in type with the same domain suffix — Claude Code
- 2026-09-19 — General summaries group key points by subject (`GeneralSummary.topics`, prompt v2): the model returns one topic per distinct subject, `keyPoints` stays the flat list for exports and search, and `KeyPointLayout` shows headings with bullets only when there are two or more titled subjects, otherwise the plain numbered list. Older summaries without `topics` decode unchanged. The overview guide asks for 5–8 sentences covering purpose, participants as labeled, subjects in order, decisions, and what stayed open — Jeremy, after his first real 25-minute meeting: "break them out into sections if there was a topic" and "the overview feels like it could be a little more robust". Grouping is the model's call but rendering is deterministic and tested; the invention rules in the shared instructions are unchanged — Jeremy / Claude Code
- 2026-09-19 — v1.1 adds only optional or defaulted stored fields (`Bookmark.kind`, `Recording.deletedAt`, `Recording.audioAvailable`, the `sample` source case) and keeps everything else inside existing JSON blobs (action-item edits in `actionItemsState`, chapter starts in the payload), so SwiftData's automatic lightweight migration covers the upgrade and no `VersionedSchema` is introduced yet — The plan (`docs/plans/2026-09-19-v1-1.md`, decision 1) proposed a `VeraFlowSchemaV2`; a versioned schema needs a frozen copy of every v1 model class, which is a lot of blind duplication for a change that is lightweight anyway. The first non-lightweight change (a renamed or removed field, a new required relationship) introduces the versioned schema — Claude Code
- 2026-09-19 — Delete moves a recording to Recently Deleted for 30 days (`Recording.deletedAt`); the row and audio stay, the Library, tags, chips and the pipeline resume ignore it, and `TrashSweeper` removes expired rows and folders at launch next to `RecordingRecovery` — A confirm-then-gone delete was the only destructive step in the app with no way back; 30 days matches what people expect from Photos and Files — Claude Code
- 2026-09-19 — "Summary ready" / "Needs attention" are local notifications with the recording's title as the whole body, requested with **provisional** authorization the first time a recording is still processing when the app leaves the foreground; a speaker-label failure never notifies (it's non-fatal), and the "ready" that follows a summary failure isn't announced as a summary — No transcript or summary text ever reaches the Lock Screen (STIG review), and provisional delivery means no permission prompt on first use; Settings → "Notify when a summary is ready" asks for full alerts — Claude Code
- 2026-09-19 — Skip silence works from 100 ms peak levels read once per file, with a threshold a little above the file's own 20th-percentile level (never an absolute number), gaps under 1.5 s ignored and 0.3 s kept on each side; ranges are cached in memory, not stored — A relative threshold works in a quiet office and on a noisy job site alike; storing the ranges would add a field for something recomputed in a second — Claude Code
- 2026-09-19 — The transcription language is a stored BCP-47 preference (`nil` = the iPhone's language) applied to new recordings and imports; "Transcribe again in…" replaces the transcript, speaker labels and summaries of one recording and re-queues it from `.recorded`; the summarizing stage checks `SystemLanguageModel.supportedLanguages` by language code first and parks the row with a clear reason — SPEC §15 asked for a picker over the supported list; a summary in an unsupported language would otherwise fail inside the model with a worse message — Claude Code
- 2026-09-19 — Marks reach the summarizer as `[mm:ss] ★ Marked: label` lines merged into the transcript in time order (prompt v3 explains the ★ line), never as a separate list or a new output field; interrupted marks stay out; the transcript shows a flag row between the paragraphs a mark fell between and exports list "Marked moments" — Answers design open question 2 with both: a line in the chunk means the model sees the mark next to what was said, and no new field means nothing new to invent — Claude Code
- 2026-09-19 — Chapters are the summary's topics (general and now client) or the walk-through's areas with a start time the model gives as mm:ss and `ChapterPostProcessor` validates (clamp, fall back to the paragraph sharing two title words, drop anything out of order or within 20 s of the previous, first at 0:00, shown only with two or more); the starts live inside the payload, not in a new store field — The model suggests, Swift decides, same rule as action-item timestamps; older payloads decode unchanged — Claude Code
- 2026-09-19 — Action-item edits (task, owner, due date, add, delete) are stored in `SummaryRecord.actionItemsState` as overrides keyed by item id, added items and removed ids; the model's payload is never modified and exports, Copy, Reminders and the library card read the resolved list; edits belong to the summary record they were made on, so a re-run with another template starts clean; a reminder already sent isn't updated in 1.1 — SPEC §11.6 says the resolved date can be edited; keeping the original output makes "what did the model say" always answerable — Claude Code
- 2026-09-19 — The library archive is a `.veraflowarchive` **package directory** (`manifest.json` + `recordings/<uuid>/recording.json` + the audio file as stored), declared as a `com.apple.package` UTType, exported through `UIDocumentPickerViewController(forExporting:asCopy:)` and imported through the Files picker or "Open in"; no zip, no dependency; existing ids are skipped unless the user replaces; a recording without audio imports with `audioAvailable = false` and its transcript and summary usable — Data portability is free and needs no network code; Apple Archive stays the fallback if Files handles the package badly on device — Claude Code
- 2026-09-19 — "Include recordings in iPhone backup" is **off** by default and flips the backup-exclusion attribute on every recording folder; the copy says the backup is Apple's and VeraFlow still sends nothing anywhere — SPEC §6.2 asked for the toggle; off keeps the promise unless the user chooses otherwise (plan decision 3) — Jeremy
- 2026-09-19 — The sample recording ships as an archive package inside a bundle folder reference (`VeraFlow/Resources/SampleRecording`), imported with `source = .sample` and the "Sample" tag from the empty Library, Settings → About, and on first launch of alpha builds; the buttons are hidden while the folder holds no archive — Reuses the import path instead of a second seeding mechanism; the content is Jeremy's to record (`README.md` in that folder says how) — Claude Code
- 2026-09-19 — Storage lives inside Settings as a group (recordings on disk, backup switch, Export library, Recently Deleted), not as its own `Features/Storage` screen — A parallel branch (`claude/confident-cori-w668u8`) built a separate `StorageView` and `StorageCleanup`; when the branches merge, Settings → Storage is the one that stays and that branch's cleanup work is folded into it rather than kept as a second entry point — Jeremy
- 2026-09-19 — Custom templates are a stored `CustomTemplate` (name, built-in base, hidden sections, focus line), unlocked-only; the summary keeps the same schema and prompt as its base, hidden sections are dropped at display and export time, and the focus line is sanitised to one plain line of at most 200 characters before it is quoted inside the FINAL instructions under the existing invention rules — A new schema per template would mean a new eval per template; hiding and focusing changes nothing the model can invent — Claude Code
- 2026-09-19 — "Ask this recording" retrieves in Swift (BM25 over paragraphs plus neighbours, within the model's context budget), makes one fresh session call per question with the excerpts as the whole prompt, and a validator keeps only citations that name a retrieved line and turns an uncited or empty answer into "not in this recording" with the closest moments; unlocked-only, except on the sample recording (plan decision 1) — Retrieval and validation are deterministic and tested; the model never sees more than the excerpts, so it can't cite what it wasn't shown — Claude Code
- 2026-09-19 — Translation uses Apple's Translation framework on device; language packs are Apple asset downloads added to SPEC §14.1's allowed list. Only prose is translated (paragraphs, titles, overviews, points, tasks, notes); owners, due phrases, dates, timestamps, measurements, quantities and addresses stay as spoken. Translations are stored per language on the paragraph and the summary record, so they survive relaunch and travel in archives; unlocked-only — The framework only hands out a session inside SwiftUI's `translationTask`, so the detail screen hosts it and a framework-free controller does the batching; keeping structured fields untouched is the same no-invention rule as everywhere else — Claude Code
- 2026-09-19 — Words while recording come from a second `SpeechAnalyzer` (volatile results) fed by a sink on the recorder's tap writer; no live speaker tags (diarization is offline), which deviates from the design's speaker-tagged live line; the preview runs only with `SpeechTranscriber`, in the foreground, below thermal `.serious`, and behind Settings → "Show words while recording" (on); nothing is persisted and the file pass after Stop makes the real transcript — Live diarization would double the CPU load during recording, the exact thing being watched on device; the sink on the writer survives capture restarts and gets nothing while paused for free — Claude Code
- 2026-09-19 — The floor drops to iOS 18: the iOS 26 services are `@available(iOS 26, *)` and `AppServices.live` picks by availability; on iOS 18–25 Parakeet transcribes, the diarizer labels, and summaries, Ask and live words report `deviceNotEligible`; Parakeet and the engine selector leave `#if DEBUG`, so release builds have one model-download path per OS (Apple assets on 26, FluidAudio on 18–25) rather than one overall (security review S-12, amended); `FoundationModels` is weak-linked in `project.yml`. Not checked in until an iOS 18 phone has run it — Jeremy asked whether older phones could use Parakeet since Apple Intelligence may not be there; the honest answer is transcripts yes, summaries no, and the app already says so — Jeremy / Claude Code
- 2026-09-19 — No bundled summary model in 1.2 (`docs/reviews/2026-09-19-bundled-model.md`): a 3 B 4-bit model is a ~1.8 GB second download, 2–6 minutes per 25-minute recording on A13–A16 with real thermal cost, weaker at "only what was said" than Apple's model, and needs MLX Swift as a second dependency; revisit after the Mac ships and the iOS 18 path has device data — Closing the gap badly would break the no-invention promise for the people least able to check — Claude Code
- 2026-09-19 — "Export library…" hands the picker the package URL without a directory trailing slash, and when iOS doesn't recognise `.veraflowarchive` as a package type it exports a zip of the package made with `NSFileCoordinator`'s upload option (Files unpacks it with a tap); Diagnostics shows whether the type is recognised — Jeremy's device: the export crashed on tap. The export picker refuses plain directories, and a directory URL (trailing slash) or an unregistered package type both look like a plain directory to it; the zip needs no dependency — Jeremy / Claude Code
- 2026-09-19 — The Mac is a native SwiftUI target (`VeraFlow-macOS`, macOS 26, same bundle identifier and product name for universal purchase), not Catalyst — Catalyst runs the iPad app and needs the iPad layout first, which is the wrong order for Jeremy's "Mac then iPad"; a native target shares every screen, view model, service protocol and the pipeline, and the platform edges are small: `Platform.swift` (device noun, settings link, a few modifier shims), `RecorderPlatform.swift` (`AVAudioSession` on iOS, Core Audio devices bound to the engine's input unit on the Mac), the export presentation (save panel and Mail's compose service instead of the share sheet and the mail composer), and the tokens' dynamic colour. Screens are never duplicated per platform.
- 2026-09-19 — Not on the Mac: the Live Activity (`NoRecordingActivityService`), `BGContinuedProcessingTask` (`InlineBackgroundProcessing`; the Mac keeps running), per-file data protection classes (`DataProtection.newFileAttributes` is empty there; files live in the sandbox container), the pasteboard's local-only/expiry options, and the privacy cover on `.inactive` — a Mac window is "inactive" whenever another app is in front, and blanking it while it sits beside that app would be wrong; the lock still engages on `.background`. Exports on the Mac go to the save panel rather than a share picker because `NSSharingServicePicker` needs an anchor view that a SwiftUI menu item doesn't provide.
- 2026-09-19 — Mac input selection rebuilds the engine (`RecorderPlatform.inputChangeNeedsRestart`): Core Audio has no "preferred input" that the running engine follows, so the chosen device is set on a fresh engine's input unit with `kAudioOutputUnitProperty_CurrentDevice`, the same restart path a route change already takes on iOS; a change while paused is applied on Resume. The device list comes from `kAudioHardwarePropertyDevices` filtered to devices with input channels, and a listener on that property reloads the menu.
- 2026-09-19 — "Ask this recording" keeps its questions with the recording (`Recording.askHistoryJSON`, the 50 most recent, in the archive too), so leaving the tab no longer loses them — An answer is worth as much as the summary and was thrown away on every navigation. Only finished exchanges are stored; a question still running or one that failed is not, and a not-found answer is kept as a question with no answer. "Transcribe again" clears the history, because the cited moments point into the transcript it replaces.
- 2026-09-19 — A question with no words of its own after stopwords ("who said that?", "when?") is treated as a follow-up: it retrieves on the previous answered exchange instead of its own words, and every question carries that exchange to the model as context. A question with a subject of its own is never a follow-up, so "was parking discussed?" still answers "not in this recording" rather than being dragged onto the previous topic. Grounding is unchanged: the answer still has to cite a retrieved moment.
- 2026-09-19 — Summary prose is editable per section (`SummaryEdits` on `SummaryRecord.summaryEditsJSON`): line replacements keyed by index, lines added by hand, and whole-paragraph replacements, applied by `SummaryPayload.applying(_:)` inside `resolvedPayload()` and `resolvedTranslation(in:)`. The model's `payloadJSON` is never written to, exactly as with action-item edits, so "what did the model actually say" stays answerable and a re-run starts clean. An index is a stable address because a payload is frozen once written — re-running makes a new `SummaryRecord`. Key points and work areas are left out of this pass: both render as groups, so a line needs a group-relative address and `KeyPointLayout` would have to report where each group came from. Empty sections don't draw a placeholder heading; an "Add a section" menu at the foot of the summary opens the editor for whatever the model left blank, which keeps a short summary short. Editing is hidden while a translation is shown, because the edits are in whatever language the user typed — Jeremy asked for per-section edit and add buttons like the action-item editor's — Jeremy / Claude Code
- 2026-09-19 — On the Mac, `AVAudioEngineConfigurationChange` only rebuilds the engine when the tap's format or the bound input device actually differs from what the engine reports now (`CaptureRestart`); `RecorderPlatform.applyInput` also leaves a device that is already bound alone — The first native Mac recordings came out empty. The log showed the engine restarting hundreds of times a second: binding the input device with `kAudioOutputUnitProperty_CurrentDevice` posts a configuration change itself, the handler answered it by rebuilding the engine, and rebuilding bound the device again. Capture never ran long enough to deliver one buffer, so `audio.aac` stayed at zero bytes and transcription failed with `kAudioFileUnspecifiedError` (2003334207). iOS never saw this because its `applyInput` is a no-op — the session owns the route. A real change always moves the format or the device, so comparing the two tells them apart without a timer or a suppression flag — Jeremy / Claude Code
- 2026-09-19 — A capture that wrote no frames fails at `stop()` with `AudioRecorderError.noAudioCaptured`, and the transcription step checks the audio file has bytes before opening it — The empty Mac recordings looked like successful ones: the Record screen showed a timer, "Audio saved" ticked, and the only sign of trouble was a Core Audio error number two steps later. `TapWriter` now keeps the first write failure instead of swallowing every one, which also separates "the file rejected the audio" from "no buffer ever arrived" — Claude Code
- 2026-09-19 — When the diarizer hears one voice on a recording longer than 45 seconds and no expected count is set, the transcript offers the count and re-labels in one tap (`SpeakerCountNotice`) — Two Mac recordings of a broadcast came back with one speaker: FluidAudio clustered 23 embedding windows 21-to-2 on the first and collapsed 60 windows to a single centroid on the second. Voices reaching one microphone through the same room and codec share more with each other than with the model's idea of a speaker, so the embeddings look alike. The `numSpeakers` hint already fixes it, but it lives in Settings where nobody finds it, and the wrong answer was silent. The notice is suppressed once the user has set a count, since repeating the advice after that helps no one — Jeremy / Claude Code
- 2026-09-19 — `LivePurchaseService` caches the entitlement: a verified, finished purchase sets it true without waiting for `Transaction.currentEntitlements`, and `Transaction.updates` rewrites it (refund, another device). `retrySummariesBlockedByFreeLimit` checks the entitlement before retrying — Jeremy bought the unlock on the Mac and the log read `unlock purchased` immediately followed by four rounds of `summary gate: unlocked false` / `summary skipped` / `prioritized`: StoreKit announced the purchase before its own entitlement query returned it, so the summary he had just paid for was refused, re-queued, and refused again. The cache makes a completed purchase authoritative; the guard means a premature announcement costs nothing instead of spinning — Jeremy / Claude Code
- 2026-09-19 — `LivePipelineCoordinator.enqueue` no longer drops a request for the recording it is already processing: it remembers it (`requeueWhenCurrentFinishes`) and puts it at the front of the queue when that pass ends; `cancel` forgets it — The guard read `current != recordingID, !queue.contains(recordingID)`, which is right for "already on its way" but wrong for "asked for again". `retry` winds the stage back first and then enqueues, so a Retry or "Label speakers again" tapped in the last moments of a pass left the recording wound back with nothing queued — a button that does nothing until the app is relaunched. `relabelKeepsSummary` caught it: the store showed `.ready` a moment before the drain loop let go. The fix has no test of its own; the window is real but too narrow to open on purpose without a seam inside the drain loop, and a test that only sometimes enters it is worse than none — Claude Code
- 2026-09-19 — Every model call caps its answer at the context budget's output reserve (`GenerationOptions.maximumResponseTokens`), and a context overflow or timeout retries with the map-reduce path forced, not only with smaller chunks — A 1:25 recording came back "too long to summarize in one pass". The budget had always subtracted a reserve (1,000 tokens on a 4K window, 1,500 above) to leave the answer room, but the calls passed `maximumResponseTokens: nil`, so the reserve was an accounting figure the model never saw: generation could run until prompt plus output overran the window and threw `exceededContextWindowSize`. Short recordings are the likeliest to hit it, because with little to say the model pads the arrays rather than stopping. The retry made it worse by looking sound: it shrank the chunk budget three times, and a transcript that fits in one call never reads the chunk budget, so all three attempts repeated the call that had just failed. Capping at the reserve makes the budget arithmetic true — instructions + schema + input + reserve is exactly the context size — so a call that fills its budget and answers to the cap still fits — Jeremy / Claude Code
- 2026-09-19 — "Expected voices" moves onto the recording (`Recording.expectedSpeakersRaw`); the Settings value is the default for recordings that don't carry one, and the one-voice notice writes to the recording instead of `UserDefaults` — Jeremy set the hint to 3 while debugging FluidAudio's clustering, and the log then read `hint exactly 3` on every recording after it, a 9-second voice memo included. Worse, the notice suppresses itself once a count is set, so the feature built to catch a collapsed diarization had silently switched itself off for every future recording the first time it was used. How many people were in a room is a fact about that meeting, not a preference. The view saves the context before asking for the relabel, because the pipeline reads the recording through its own `ModelContext` — Jeremy / Claude Code
