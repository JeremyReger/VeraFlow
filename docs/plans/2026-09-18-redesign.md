# Redesign plan — Chalk / Slate (design package, 2026-09-18)

Jeremy supplied `docs/design/DESIGN_SPEC.md`, `docs/design/tokens.json`, and the Swift tokens and
components now in `VeraFlow/Design/`. The rule from the package: screens reference token names,
never hex values; both appearances follow the system setting.

## Phase 1 — foundation (done)

- Fonts: Newsreader Regular/Italic and Manrope 400/500/600/700, static instances built from
  Google's variable fonts (`scripts` note in DECISIONS), bundled under the OFL with the licence
  texts, registered in `UIAppFonts`. PostScript names verified to match `VFFontName`.
- `VeraFlowTokens.swift` and `VeraFlowComponents.swift` in the app target, plus the shared
  pieces the screens need (`VFReassurance`, `VFWaveformMark`, `VFScreenHeader`, `VFField`,
  `VFRoundButtonLabel`); `VFLiveChip` honours Reduce Motion.
- Colours: the accent asset now carries the token pair; the interim `Alert` and `Speaker1–8`
  assets are gone (`VFColor.danger`, `VFColor.speaker`).
- Icon: "Ember" per spec §7 (generated by `scripts/icon/make_icon.py`; alpha variant keeps the band).

## Phase 2 — screens, one commit each (all written 2026-09-18; awaiting Jeremy's build)

1. **Library + first run** (done): hidden system bar, eyebrow/serif header, round search /
   settings / more buttons, inline search field, chips (All, Starred, templates in use, tags),
   day-grouped cards (`LibraryCardModel`, `LibraryGrouping`), pinned Record pill + import button
   over a scrim, first-run screen with the waveform mark and the privacy line.
2. Recording detail shell (done): serif title, metadata line, underline tabs, mini player with the speed
   pill pinned under Summary and Transcript; star / share / more in the bar.
3. Summary (done): template chip + Change, serif overview, numbered key points with hairlines, action
   items as checkboxes (done state strikes through and drops to tertiary).
4. Transcript (done): search field + speaker filter, speaker tag (dot + name + timecode + `NAME?` chip),
   serif paragraphs, now-playing block with tinted fill and border.
5. Record ready / running / paused (done; template picker included): close button, eyebrow, ring + accent circle, input and
   template picker rows, privacy line; live chip, serif tabular timer, waveform, MARK / stop /
   PAUSE transport; paused state per §5.
6. Audio tab (done; `AudioTab`, `WaveformPeaks`, `TalkTime`): waveform scrubber with playhead, ±15 s transport with speed pill, per-voice talk
   time with rename, file details, export buttons.
7. Settings (done): serif title with Done pill, three hairline card groups, italic privacy footer.
8. Processing card (done; `ProcessingSteps`, `ProcessingCard`): stages with a determinate bar, honest background copy.
9. Onboarding, consent sheet, paywall, lock and shield covers restyled with the tokens (done).

## Phase 3 — features the spec implies (Jeremy decides)

- Generated titles and gists: already available from the summary (`SummaryPayload.title`,
  `overview`), so the library uses them with no extra model call (spec open question 1).
- Mark button = the existing Bookmark; the summary doesn't weight marks yet (open question 2).
- Live transcript while recording (§4 Record — running): logged as a future request in PROGRESS.
- Template picker before recording: done (`RecorderViewModel.selectedTemplate`).
- Playback speed: done (`AVAudioPlayer.enableRate`, 1× / 1.5× / 2× cycle).
- Export menu design (open question 3): the existing Share menu stays until designed.

## Verification

- `scripts/test.sh` after each phase-2 commit; `#Preview` blocks in light and dark.
- Device: fonts render (Newsreader visible on titles; if San Francisco shows, the PostScript
  names didn't match), light and dark, Dynamic Type at AX3 on the library card and settings rows.
